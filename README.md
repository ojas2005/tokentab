# tokentab

Cost estimation and budget enforcement for LLM API calls.

`tokentab` wraps LLM API calls to estimate cost before the request goes out,
enforce a budget, and record what was actually spent. Zero required
dependencies, no API key of its own.

Work in progress.
